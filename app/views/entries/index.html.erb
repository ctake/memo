<h1>メモリスト</h1>

<table>
  <tr>
    <th>タイトル</th>
    <th>本文</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @entries.each do |entry| %>
  <tr>
    <td><%= entry.title %></td>
    <td><%= entry.body %></td>
    <td><%= link_to '見る', entry %></td>
    <td><%= link_to '編集', edit_entry_path(entry) %></td>
    <td><%= link_to '削除', entry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '新規作成', new_entry_path %>
